# Timer UI

## Purpose
Design and implement a minimal, readable timer display and controls.

## Tasks
- Display the current timer value (large, readable)
- Add start, pause, and reset buttons
- Ensure UI is accessible (keyboard, screen reader)

## Acceptance Criteria
- Timer value is always visible and easy to read
- Controls are intuitive and accessible
- UI is visually clean and minimal
